# Improved Settings 0.1.1

Remove the extra clear buttons beside the Modules and Settings fields. Both fields retain their built-in clear X and Escape shortcut.

Verified with eight passing automated tests and the installed 0.1.1 release on Forge v14.364. The rendered form contains no separate clear buttons. Clicking the built-in Modules X clears the filter, and Escape clears the Settings search.
